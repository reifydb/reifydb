[package]
name = "reifydb-build"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Build utilities for ReifyDB target detection"

[dependencies]
# No dependencies needed - pure build-time logic
