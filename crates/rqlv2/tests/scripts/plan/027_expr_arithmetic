# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright (c) 2025 ReifyDB

# Test arithmetic expressions
plan 'from test.orders | map { sum: amount + 10, diff: amount - 5, prod: amount * 2, quot: amount / 2 }'
---
PROJECT
  output: [(amount + 10) AS sum, (amount - 5) AS diff, (amount * 2) AS prod, (amount / 2) AS quot]
schema:
    sum: Any
    diff: Any
    prod: Any
    quot: Any
  SCAN test.orders
  schema:
      id: Int8
      user_id: Int8
      amount: Float8
