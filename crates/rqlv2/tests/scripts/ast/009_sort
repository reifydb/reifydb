# SPDX-License-Identifier: AGPL-3.0-or-later
# Copyright (c) 2025 ReifyDB

# Test SORT with ASC/DESC
parse 'FROM users | SORT { age: DESC, name: ASC }'
---
> Program
> `-- [0] Pipeline
>     +-- [0] From(users)
>     `-- [1] Sort
>         +-- [0] SortColumn DESC
>         |   `-- Identifier(age)
>         `-- [1] SortColumn ASC
>             `-- Identifier(name)
>

# Test SORT with single column (defaults to ASC)
parse 'FROM users | SORT { name }'
---
> Program
> `-- [0] Pipeline
>     +-- [0] From(users)
>     `-- [1] Sort
>         `-- [0] SortColumn
>             `-- Identifier(name)
>

# Test SORT with single column explicit ASC
parse 'FROM users | SORT { name: ASC }'
---
> Program
> `-- [0] Pipeline
>     +-- [0] From(users)
>     `-- [1] Sort
>         `-- [0] SortColumn ASC
>             `-- Identifier(name)
>

# Test SORT with single column DESC
parse 'FROM users | SORT { name: DESC }'
---
> Program
> `-- [0] Pipeline
>     +-- [0] From(users)
>     `-- [1] Sort
>         `-- [0] SortColumn DESC
>             `-- Identifier(name)
>
