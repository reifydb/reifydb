[package]
name = "reifydb-testing"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
reifydb-core = { workspace = true }
reifydb-type = { workspace = true }

regex = { workspace = true, features = [] }

[build-dependencies]
# Can't use self as build dependency, need to duplicate code

[dev-dependencies]