[package]
name = "reifydb-uptime-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "POC: Node native addon exposing uptime's real DST-mode engine for web tests"
publish = false

[lib]
name = "bridge"
crate-type = ["cdylib"]

[dependencies]
reifydb-node = { version = "0.9.0", path = "../../../rust/reifydb-node" }
reifydb-uptime-backend = { path = "../backend" }
napi = { workspace = true }
napi-derive = { workspace = true }

[build-dependencies]
napi-build = { workspace = true }
reifydb-build = { workspace = true }
