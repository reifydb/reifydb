# Verify each test file gets a fresh database.
# If the DB were shared across files, this namespace might already exist.

CREATE NAMESPACE isolated;
CREATE TABLE isolated::data { id: int4 };
INSERT isolated::data [{ id: 42 }];

CREATE TEST isolated::db_is_clean {
    FROM isolated::data | ASSERT { id == 42 }
};

RUN TESTS
