# Ensure workspace packages are linked properly during development
link-workspace-packages=true

# Use a shared lockfile for the workspace
shared-workspace-lockfile=true

# Allow publishing from any branch
publish-branch=*

# Auto-install peers
auto-install-peers=true